/* Placeholder for HTMX. Real vendored copy will be fetched by
 * scripts/build_tailwind.sh (or a dedicated vendor script) in M1/M2 when
 * the first HTMX interactions arrive. Kept here so url_for('static',
 * path='vendor/htmx.min.js') does not 404 during bootstrap smoke tests.
 */
